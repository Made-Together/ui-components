---
"@togetheragency/ui": patch
---

Updated default behaviour of <NumberFlow /> component; it now uses 'continuous' plugin by default. It also now accepts a new prop called 'animateInView' which allows you to animate the number when it is in view.
